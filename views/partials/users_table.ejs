<table class="table table-striped table-info table-border table-hover">
    <thead class="thead-light">
        <tr>
            <th scope="col">Foto</th>
            <th scope="col">ID</th>
            <th scope="col">Usuário</th>
            <th scope="col">Senha</th>
        </tr>
    </thead>
    <tbody>
        <% for (let i = 0; i < row.length; i++) { %>
        <% if (row[i].username == req.session.username) { %>
        <tr class="bg-primary">
            <% } else { %>
        </tr>
        <tr>
            <% } %>
            <th scope="row"><img src="/static/logo-invertido.png" width="32px" height="32px" /></th>
            <th>
                <%= row[i].id %>
            </th>
            <td>
                <%= row[i].username %>
            </td>
            <td>
                <%= row[i].password %>
            </td>
        </tr>
        <% } %>
    </tbody>
</table>